[project]
name = "moodtracker-scripts"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "boto3>=1,<2",
    "checkov>=3.2.459",
    "troposphere[policy]>=4.10.1",
    "cfn-lint>=1.26.1",
]

[tool.uv]
package = false
