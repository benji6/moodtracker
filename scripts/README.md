# Scripts

Place to keep Python scripts.

## Getting started

- You can install dependencies using [uv](https://docs.astral.sh/uv/)
- You can run the scripts within this directory like this: `uv run python3 ./analytics.py`
- You may need AWS credentials with the correct permissions set up to run some of these
